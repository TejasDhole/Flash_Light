<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_above="@+id/flashlightButton"

        android:layout_alignStart="@+id/flashlightButton"
        android:layout_alignEnd="@+id/flashlightButton"
        android:layout_alignParentTop="true"
        android:layout_marginStart="27dp"
        android:layout_marginTop="179dp"
        android:layout_marginEnd="39dp"
        android:layout_marginBottom="62dp"
        android:src="@drawable/baseline_flashlight_on_24" />

    <Button
        android:id="@+id/flashlightButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Turn On Flashlight"
        android:layout_centerInParent="true"
        android:onClick="turnOnFlashlight" />
</RelativeLayout>
